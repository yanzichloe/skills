# Figma Variable 资产说明

本目录用于存放从 Figma 导出的变量文件、变量截图和导入说明。

当前文件包已在 `assets/json-token/raw/` 中保留原始导出文件：

- `色阶.json`
- `色彩 .json`
- `单元 .json`

建议后续补充：

```text
figma-variable/
├── qieman-color-variable.fig
├── qieman-unit-variable.fig
├── variable-screenshot-light.png
└── variable-screenshot-dark.png
```
